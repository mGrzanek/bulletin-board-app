// selectors

// actions
const createActionName = actionName => `app/requests/${actionName}`;

// action creators


// reducer
const requestsReducer = (statePart = [], action) => {
    switch(action.type) {
        default:
            return statePart;
    }
}

export default requestsReducer;